---
import { rodape } from '../data/nav';
import { existe } from '../data/rotas';
---
<footer class="footer">
  <div class="container">
    <div class="footer__grid">
      <div class="footer__col">
        <img class="logo__img logo__img--rodape" src="/marca/renke-assinatura.png" alt="Renke Studio" width="299" height="62" />
      </div>

      <nav class="footer__col" aria-label="Links do rodapé">
        <p class="footer__title">Links</p>
        <ul>
          {/* Sem "em breve" à mostra (Lenora, 26/09): só o que já tem página. */}
          {rodape.links.filter((l) => existe(l.rota)).map((l) => (
            <li><a href={l.rota}>{l.rotulo}</a></li>
          ))}
        </ul>
      </nav>

      <div class="footer__col">
        <p class="footer__title">Siga-nos</p>
        <ul>
          {rodape.redes.filter((r) => r.rota && r.rota !== '#').map((r) => (
            <li><a href={r.rota} rel="noopener">{r.rotulo}</a></li>
          ))}
        </ul>
      </div>

      <div class="footer__col">
        <p class="footer__title">Contato</p>
        <ul class="footer__contact">
          <li>{rodape.endereco.map((linha, i) => <>{i > 0 && <br />}{linha}</>)}</li>
          <li>{rodape.email}</li>
        </ul>
      </div>
    </div>

    <div class="footer__bottom">
      <p>{rodape.copyright}</p>
      <p><em>{rodape.assinatura}</em></p>
    </div>
  </div>
</footer>
